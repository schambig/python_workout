'''
Inmmutable data type!
Tuples are zero indexed!
We can store multiple data types into tuples
We use parenthesis to use tuples
(kinda lists but lists are mutable data types)
'''

#           0       1   2       3       4           5 
tupla = ('String', 10, 3.1415, True, [1, 2, 3], (4, 5, 6))

print(tupla)
print(type(tupla))
